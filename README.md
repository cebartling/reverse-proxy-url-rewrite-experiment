reverse-proxy-url-rewrite-experiment
====================================

An experiment around using a reverse proxy and URL rewriting to insulate a collection of micro-service processes behind a logical API abstraction.
